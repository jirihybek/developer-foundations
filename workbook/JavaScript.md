# JavaScript

Below are workbook questions related to JavaScript. Write down your findings and answers to the questions.

---

## Language Fundamentals

#### 1. What is ECMAScript? How does it relate to JavaScript?

(answer here)

#### 2. What are the primitive types in JavaScript? What is the difference between primitive types and objects?

(answer here)

#### 3. What is the difference between `null` and `undefined`? When would you use each?

(answer here)

#### 4. Is a function an object in JavaScript? Why or why not? What implications does this have?

(answer here)

#### 5. What are prototypes in JavaScript? How does prototypal inheritance work?

(answer here)

#### 6. What is the prototype chain? What happens when you access a property that doesn't exist on an object?

(answer here)

#### 7. What are template literals (template strings)? How do they differ from regular strings? What can you do with them?

(answer here)

---

## Type Coercion and Comparison Quirks

#### 7. What is the difference between `==` and `===`? Why is `===` generally preferred?

(answer here)

#### 8. What are some famous JavaScript comparison quirks? Explain why these happen:
- `typeof null === "object"`
- `[] == false` but `[] ? true : false` evaluates to `true`
- `"10" + 1` vs `"10" - 1`
- `NaN === NaN`

(answer here)

#### 9. What is type coercion? What is the difference between implicit and explicit coercion?

(answer here)

#### 10. What does `typeof` return for different values? Are there any surprising results?

(answer here)

---

## Variables and Scope

#### 11. What is the difference between `var`, `let`, and `const`? Why is `var` considered problematic?

(answer here)

#### 12. What is hoisting? How does it differ for `var`, `let`, `const`, and function declarations?

(answer here)

#### 13. What is the Temporal Dead Zone (TDZ)?

(answer here)

#### 14. What is a closure? Why are closures useful?

(answer here)

#### 15. What is the difference between lexical scope and dynamic scope? Which one does JavaScript use?

(answer here)

---

## Functions

#### 16. What are the different ways to define a function in JavaScript? (function declaration, expression, arrow function, etc.)

(answer here)

#### 17. What is the difference between arrow functions and regular functions? (hint: think about `this`, `arguments`, constructors)

(answer here)

#### 18. What is `this` in JavaScript? How is it determined? What are the rules for `this` binding?

(answer here)

#### 19. What do `call`, `apply`, and `bind` do? What are the differences between them?

(answer here)

#### 20. What are higher-order functions? Give examples of built-in higher-order functions.

(answer here)

---

## Loops and Iteration

#### 21. What types of loops does JavaScript have? (`for`, `for...in`, `for...of`, `while`, `do...while`, `forEach`) When to use each?

(answer here)

#### 22. What is the difference between `for...in` and `for...of`? When would you use each?

(answer here)

#### 23. What is an iterator? What is the iterator protocol? What makes an object iterable?

(answer here)

---

## Asynchronous JavaScript

#### 24. What is the event loop? How does it work? Why is it important to understand for JavaScript developers?

(answer here)

#### 25. What is the difference between the call stack, task queue (callback queue), and microtask queue?

(answer here)

#### 26. What are callbacks? What is "callback hell" and how can you avoid it?

(answer here)

#### 27. What is a Promise? What are the three states of a Promise?

(answer here)

#### 28. What is `async/await`? How does it relate to Promises? What are the benefits of using it?

(answer here)

#### 29. What is `Promise.all`, `Promise.race`, `Promise.allSettled`, and `Promise.any`? When would you use each?

(answer here)

#### 30. What happens if you don't handle a rejected Promise? What is an unhandled rejection? How does the bahavior differ between environments (Node.js vs browser)?

(answer here)

---

## Objects and Arrays

#### 31. What are the different ways to create an object in JavaScript?

(answer here)

#### 32. What is destructuring? How does it work for objects and arrays?

(answer here)

#### 33. What is the spread operator (`...`)? What are its use cases?

(answer here)

#### 34. What is the difference between shallow copy and deep copy? How do you create each in JavaScript?

(answer here)

#### 35. What are `Map` and `Set`? How do they differ from plain objects and arrays?

(answer here)

---

## Modules

#### 36. What are ES Modules? What is the difference between `import/export` and CommonJS (`require/module.exports`)?

(answer here)

#### 37. What is the difference between named exports and default exports?

(answer here)

---

## Engines and Runtimes

#### 38. What is a JavaScript engine? Name some commonly used engines (e.g., V8, SpiderMonkey, JavaScriptCore).

(answer here)

#### 39. What is a JavaScript runtime? What does it provide on top of the engine?

(answer here)

#### 40. What are the main JavaScript runtimes? (browser, Node.js, Deno, Bun) What are the key differences?

(answer here)

#### 41. What is browser-specific API vs. standard ECMAScript API? Give examples of each.

(answer here)

#### 42. How does writing JavaScript for the back-end differ from writing JavaScript for the browser?

(answer here)

---

## Error Handling

#### 43. How does `try/catch/finally` work in JavaScript?

(answer here)

#### 44. What is the difference between throwing an `Error` object vs. throwing a string or other value?

(answer here)

#### 45. How do you handle errors in asynchronous code (callbacks, Promises, async/await)?

(answer here)
